 ## Customize the test machine
machine:
  services:
    - docker
    
dependencies:
  overide:
    - openssl aes-256-cbc -d -in ./secret.env -k $SECRET_CIRCLE_DECRYPT_KEY >> ~/.circlerc
    - sudo pip install --upgrade docker-compose

test:
  overide:
    - ./run_unit_tests.sh build
